import React,{useState}from'react';import'../panelDynamic.css';export default function Document(){const[d,setD]=useState([]);const upload=e=>{const f=e.target.files?.[0];if(f)setD([...d,{id:Date.now(),name:f.name,size:Math.ceil(f.size/1024)+' KB',status:'Uploaded'}])};return <section className='panel-page'><h1 className='panel-title'>Documents</h1><div className='panel-card'><h3>Upload document</h3><p className='muted'>Upload invoices, ID proof or shipment-related documents.</p><input type='file' onChange={upload}/></div><div className='panel-list' style={{marginTop:18}}>{d.length?d.map(x=><div className='panel-item' key={x.id}><div><b>{x.name}</b><p className='muted'>{x.size}</p></div><span className='badge'>{x.status}</span></div>):<div className='panel-card muted'>No documents uploaded yet.</div>}</div></section>}